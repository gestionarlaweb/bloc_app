import 'package:flutter/material.dart';

class DetailsGif extends StatelessWidget {
  const DetailsGif({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('Detalle Gif')),
        body: Center(
          child: Text('text'),
        ));
  }
}
